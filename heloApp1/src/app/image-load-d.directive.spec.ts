import { ImageLoadDDirective } from './image-load-d.directive';

describe('ImageLoadDDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageLoadDDirective();
    expect(directive).toBeTruthy();
  });
});
